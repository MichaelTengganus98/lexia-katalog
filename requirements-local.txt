# Local development pins (Python 3.8) — mirrors the production stack (Django 2.1)
# without the cPanel-only pieces (mysqlclient, gunicorn, memcached, social-auth).
Django==2.1.15
pytz==2024.1
six==1.16.0
dj-database-url==0.5.0
psycopg2-binary==2.9.9
Pillow==9.5.0
sorl-thumbnail==12.5.0
django-cleanup==4.0.0
whitenoise==5.3.0
